import React, { useState } from 'react';
import CleanStudentDashboard from './CleanStudentDashboard';

export default function StudentDashboard(props) {
  return <CleanStudentDashboard {...props} />;
}
